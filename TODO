Maintenance:
    ☐ switch to `nodejs22.x`
    ☐ add bundler ( `@vercel/ncc` ? or `vite`)
    ☐ update serverless
    ☐ switch config to abap-version-shield.sbcg.com.ua (+ certificate)
    ☐ add `version` or `about` endpoint (name, version, web-url)

Update dependencies:
    ☐ The querystring API is considered Legacy. new code should use the URLSearchParams API instead
    ☐ Upgrade to v7.0.2+ of superagent

Updates:
    ☐ Upgrade to esm
    ☐ Maybe switch to TS + vite (or esbuild) + vitest
    ☐ remove root path (version-shield-json)